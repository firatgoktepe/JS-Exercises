Build a function that, given a list of values, returns a dictionary with the number of occurrences of each value in the list. 

For Example: 

    values = ["a", "a", "b", "c", "b"]

Returns:

    {
        "a": 2,
        "c": 1,
        "b": 2
    }